name: slang_package
description: "slang Flutter project."
version: 0.0.1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  slang: ^3.30.2
  slang_flutter: ^3.30.0

dev_dependencies:
  build_runner: ^2.4.9
  slang_build_runner: ^3.30.0

flutter: