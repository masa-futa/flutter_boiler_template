name: isar_package
description: "isar Flutter project."
version: 0.0.1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  isar:
  isar_flutter_libs:
  path_provider:

dev_dependencies:
  build_runner: ^2.4.9
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  isar_generator:

flutter:
